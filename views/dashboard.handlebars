{{#if jokes.length}}
<section>
    <h2>Jokes:</h2>
     {{#each jokes as |joke|}}   
    <h4>{{joke.joke_text}}</h4>
        
{{/each}}
</section>
{{/if}}

{{!-- <button type="submit" class="btn"><a class ="link" href="/dashboard/new">New Joke</a></button>
<script src="/js/newJoke.js"></script> --}}